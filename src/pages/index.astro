---
import BaseLayout from '../layouts/BaseLayout.astro';

const CDN = 'https://raw.githubusercontent.com/pcmcdiary-hue/pcmcdiary-data-v2/main/data';

const iconMap = {
  'Food & Dining': 'fa-utensils','Grocery & Kirana': 'fa-cart-shopping','Health & Medical': 'fa-stethoscope',
  'Beauty & Personal Care': 'fa-spa','Fitness & Wellness': 'fa-dumbbell','Education & Learning': 'fa-graduation-cap',
  'Fashion & Apparel': 'fa-shirt','Jewelry & Gold': 'fa-gem','Electronics & Gadgets': 'fa-laptop',
  'Home & Interior': 'fa-couch','Automotive': 'fa-car','Domestic Services': 'fa-broom',
  'IT & Digital Services': 'fa-code','Logistics & Movers': 'fa-truck-fast','Hospitality & Travel': 'fa-plane',
  'Entertainment & Events': 'fa-film','Kids & Baby': 'fa-child','Books & Stationery': 'fa-book',
  'Gardening & Outdoor': 'fa-seedling','Pet Care': 'fa-paw','Real Estate & Property': 'fa-building',
  'Finance & Legal': 'fa-coins','Packaging & Printing': 'fa-print','Cafes & Quick Bites': 'fa-mug-hot',
  'Shopping & Retail': 'fa-bag-shopping','Communication & Internet': 'fa-wifi','Security Services': 'fa-shield-halved',
  'Alternative Medicine': 'fa-leaf','Photography & Video': 'fa-camera','Celebrations & Gifts': 'fa-gift',
  'Government Services': 'fa-landmark','Utilities': 'fa-bolt','Daily Use Services': 'fa-screwdriver-wrench',
  'Fitness Equipment': 'fa-person-running','Ayurveda & Wellness': 'fa-mortar-pestle','Coaches & Trainers': 'fa-chalkboard-user',
  'Interior & Exterior Design': 'fa-paint-roller','Consulting Services': 'fa-handshake','Specialty Stores': 'fa-store'
};

const experts = [
  { name: 'Dr. Saala', cat: 'Health & Medical', icon: 'fa-heart-pulse', grad: 'linear-gradient(135deg,#ef4444,#b91c1c)', sample: '5 early signs of diabetes you should not ignore', slug: 'dr-saala', readers: '8.4k' },
  { name: 'Arthik Saala', cat: 'Finance & Tax', icon: 'fa-coins', grad: 'linear-gradient(135deg,#10b981,#047857)', sample: 'How to save ₹46,800 tax this year (legally)', slug: 'arthik-saala', readers: '6.2k' },
  { name: 'Vastu Saala', cat: 'Vastu & Harmony', icon: 'fa-compass', grad: 'linear-gradient(135deg,#8b5cf6,#5b21b6)', sample: 'Kitchen direction: why it affects your relationships', slug: 'vastu-saala', readers: '5.1k' },
  { name: 'Fit Saala', cat: 'Fitness & Nutrition', icon: 'fa-dumbbell', grad: 'linear-gradient(135deg,#f97316,#c2410c)', sample: 'Lose 5 kg in 8 weeks — without giving up roti', slug: 'fit-saala', readers: '7.3k' },
  { name: 'Kanoon Saala', cat: 'Legal & Rights', icon: 'fa-scale-balanced', grad: 'linear-gradient(135deg,#334155,#0f172a)', sample: 'Rent agreement: 7 clauses you must check', slug: 'kanoon-saala', readers: '4.8k' },
  { name: 'Rashi Saala', cat: 'Astrology', icon: 'fa-star', grad: 'linear-gradient(135deg,#a855f7,#7c3aed)', sample: 'Your weekly rashi phal — career & health', slug: 'rashi-saala', readers: '9.1k' }
];

let categories = [];
let locations = [];

try {
  const catRes = await fetch(`${CDN}/categories.json`);
  const catData = await catRes.json();
  categories = (catData.categories || []).map(c => ({ ...c, iconClass: iconMap[c.categoryName] || 'fa-folder' }));
} catch (e) { console.error(e); }

try {
  const locRes = await fetch(`${CDN}/locations.json`);
  const locData = await locRes.json();
  locations = locData.locations || [];
} catch (e) { console.error(e); }
---

<BaseLayout title="Home" description="PCMC Diary — Find trusted local businesses in Pimpri-Chinchwad.">

  <!-- ════════════════════════════════════════
       HERO
       ════════════════════════════════════════ -->
  <section class="hero">
    <div class="hero-mesh"></div>
    <div class="hero-grain"></div>
    <div class="container hero-content">
      <div class="hero-badge">
        <span class="pulse-dot"></span>
        <span>Trusted by 10,000+ locals in PCMC</span>
      </div>

      <h1 class="hero-title">
        The smarter way to discover<br>
        <span class="hero-accent">Pimpri-Chinchwad</span>
      </h1>

      <p class="hero-sub">
        Verified businesses, exclusive coupons &amp; expert advice — all in one place.
      </p>

      <form class="hero-search" action="/directory" method="get">
        <div class="hs-icon"><i class="fa-solid fa-magnifying-glass"></i></div>
        <input type="search" name="q" placeholder="Search restaurants, gyms, doctors, CA…" />
        <button type="submit">
          Search
          <i class="fa-solid fa-arrow-right"></i>
        </button>
      </form>

      <div class="hero-quick">
        <span class="hero-quick-label">Popular:</span>
        <a href="/category/CAT001">Food</a>
        <a href="/category/CAT005">Fitness</a>
        <a href="/category/CAT003">Doctors</a>
        <a href="/category/CAT004">Salons</a>
        <a href="/category/CAT002">Grocery</a>
      </div>
    </div>

    <!-- Trust strip -->
    <div class="container">
      <div class="trust-strip">
        <div class="trust-item">
          <div class="trust-num">{categories.length}<span>+</span></div>
          <div class="trust-lbl">Categories</div>
        </div>
        <div class="trust-div"></div>
        <div class="trust-item">
          <div class="trust-num">{locations.length}</div>
          <div class="trust-lbl">Areas covered</div>
        </div>
        <div class="trust-div"></div>
        <div class="trust-item">
          <div class="trust-num">500<span>+</span></div>
          <div class="trust-lbl">Listed businesses</div>
        </div>
        <div class="trust-div"></div>
        <div class="trust-item">
          <div class="trust-num">21</div>
          <div class="trust-lbl">Expert columns</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       POPULAR CATEGORIES
       ════════════════════════════════════════ -->
  <section class="section">
    <div class="container">
      <div class="sec-head">
        <div>
          <span class="eyebrow">Explore</span>
          <h2 class="sec-title">Browse by category</h2>
          <p class="sec-sub">All {categories.length} categories across every corner of PCMC</p>
        </div>
        <a href="/directory" class="sec-link">
          See all
          <i class="fa-solid fa-arrow-right"></i>
        </a>
      </div>

      <div class="cat-grid">
        {categories.slice(0, 18).map((cat, i) => (
          <a href={`/category/${cat.categoryId}`} class="cat-card">
            <div class="cat-icon-wrap">
              <i class={`fa-solid ${cat.iconClass}`}></i>
            </div>
            <div class="cat-name">{cat.categoryName}</div>
          </a>
        ))}
      </div>

      {categories.length > 18 && (
        <div class="cat-more">
          <a href="/directory" class="btn-ghost">
            View all {categories.length} categories
            <i class="fa-solid fa-arrow-right"></i>
          </a>
        </div>
      )}
    </div>
  </section>

  <!-- ════════════════════════════════════════
       EXPERT SALLA — EDITORIAL SHOWCASE
       ════════════════════════════════════════ -->
  <section class="section section-expert">
    <div class="container">
      <div class="sec-head">
        <div>
          <span class="eyebrow eyebrow-gold">Editorial</span>
          <h2 class="sec-title">PCMC <em>Expert Salla</em></h2>
          <p class="sec-sub">Real advice from doctors, CAs, trainers, astrologers &amp; more</p>
        </div>
        <a href="/expert-salla" class="sec-link sec-link-gold">
          All experts
          <i class="fa-solid fa-arrow-right"></i>
        </a>
      </div>

      <!-- Featured expert -->
      <div class="ex-featured">
        <div class="ex-featured-left">
          <div class="ex-featured-pill">
            <i class="fa-solid fa-crown"></i>
            Column of the month
          </div>
          <h3 class="ex-featured-title">
            Dr. Saala — <em>Your weekly health companion</em>
          </h3>
          <p class="ex-featured-desc">
            Practical medical advice for PCMC families. Seasonal tips, home remedies, when to see a doctor &amp; how to read reports — in plain language.
          </p>
          <div class="ex-featured-meta">
            <span><i class="fa-solid fa-calendar"></i> Weekly</span>
            <span><i class="fa-solid fa-eye"></i> 8,400+ readers</span>
            <span><i class="fa-solid fa-user-doctor"></i> Verified MD</span>
          </div>
          <a href="/search/label/dr-saala" class="ex-featured-btn">
            <i class="fa-solid fa-book-open"></i>
            Read latest article
          </a>
        </div>
        <div class="ex-featured-right">
          <div class="ex-stat">
            <i class="fa-solid fa-users"></i>
            <div>
              <div class="ex-stat-num">21</div>
              <div class="ex-stat-lbl">Expert columns</div>
            </div>
          </div>
          <div class="ex-stat">
            <i class="fa-solid fa-newspaper"></i>
            <div>
              <div class="ex-stat-num">500+</div>
              <div class="ex-stat-lbl">Articles published</div>
            </div>
          </div>
          <div class="ex-stat">
            <i class="fa-solid fa-hand-holding-dollar"></i>
            <div>
              <div class="ex-stat-num">₹2,999</div>
              <div class="ex-stat-lbl">Sponsor / month</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Experts grid -->
      <div class="ex-grid">
        {experts.map(ex => (
          <a href={`/search/label/${ex.slug}`} class="ex-card">
            <div class="ex-card-av" style={`background:${ex.grad}`}>
              <i class={`fa-solid ${ex.icon}`}></i>
            </div>
            <div class="ex-card-body">
              <div class="ex-card-name">{ex.name}</div>
              <div class="ex-card-sample">"{ex.sample}"</div>
              <div class="ex-card-foot">
                <span class="ex-card-cat">{ex.cat}</span>
                <span class="ex-card-read">{ex.readers} <i class="fa-solid fa-arrow-right"></i></span>
              </div>
            </div>
          </a>
        ))}
      </div>

      <!-- Sponsor CTA -->
      <div class="ex-cta">
        <div class="ex-cta-glow"></div>
        <div class="ex-cta-inner">
          <div class="ex-cta-icon"><i class="fa-solid fa-bullhorn"></i></div>
          <div class="ex-cta-text">
            <h4>Become an expert writer or sponsor a column</h4>
            <p>Reach 10,000+ engaged PCMC readers every month with expert articles.</p>
          </div>
          <a href="/plans" class="ex-cta-btn">
            See packages
            <i class="fa-solid fa-arrow-right"></i>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       AREAS
       ════════════════════════════════════════ -->
  <section class="section">
    <div class="container">
      <div class="sec-head">
        <div>
          <span class="eyebrow">Neighbourhoods</span>
          <h2 class="sec-title">Every area of PCMC</h2>
          <p class="sec-sub">{locations.length} areas from Hinjawadi to Chakan</p>
        </div>
        <a href="/directory" class="sec-link">
          Full directory
          <i class="fa-solid fa-arrow-right"></i>
        </a>
      </div>

      <div class="loc-grid">
        {locations.map(loc => (
          <a href={`/location/${loc.locationId}`} class="loc-chip">
            <span class="loc-dot"></span>
            <span class="loc-name">{loc.locationName}</span>
            <i class="fa-solid fa-arrow-right loc-arrow"></i>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       FINAL CTA
       ════════════════════════════════════════ -->
  <section class="section">
    <div class="container">
      <div class="final-cta">
        <div class="final-cta-glow-1"></div>
        <div class="final-cta-glow-2"></div>
        <div class="final-cta-inner">
          <div class="final-icon"><i class="fa-solid fa-store"></i></div>
          <h2>Own a business in PCMC?</h2>
          <p>Get discovered by thousands of local customers. Free listings available — upgrade anytime for premium visibility.</p>
          <div class="final-cta-btns">
            <a href="/register" class="btn-primary">
              <i class="fa-solid fa-circle-plus"></i>
              List your business — free
            </a>
            <a href="/plans" class="btn-outline">
              <i class="fa-solid fa-crown"></i>
              View plans
            </a>
          </div>
          <div class="final-cta-note">
            <i class="fa-solid fa-shield-halved"></i>
            No credit card required · Free forever plan
          </div>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style is:global>
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap');

:root {
  --brand: #b91c1c;
  --brand-dark: #991b1b;
  --brand-light: #fca5a5;
  --brand-bg: #fef2f2;
  --gold: #d97706;
  --gold-dark: #b45309;
  --gold-bg: #fffbeb;
  --ink: #0f172a;
  --ink-2: #1e293b;
  --ink-3: #334155;
  --slate: #475569;
  --muted: #64748b;
  --muted-2: #94a3b8;
  --line: #e2e8f0;
  --line-2: #f1f5f9;
  --paper: #fdfcfb;
  --white: #ffffff;
  --font: 'Plus Jakarta Sans', system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
  --shadow-sm: 0 1px 2px rgba(15,23,42,0.04);
  --shadow-md: 0 4px 12px -4px rgba(15,23,42,0.06), 0 2px 6px -2px rgba(15,23,42,0.04);
  --shadow-lg: 0 20px 40px -16px rgba(15,23,42,0.12), 0 8px 16px -8px rgba(15,23,42,0.06);
  --shadow-brand: 0 12px 32px -10px rgba(185,28,28,0.28);
  --shadow-gold: 0 12px 32px -10px rgba(217,119,6,0.28);
  --tr: cubic-bezier(0.4, 0, 0.2, 1);
  --tr-out: cubic-bezier(0.16, 1, 0.3, 1);
}

body { font-family: var(--font); -webkit-font-smoothing: antialiased; }
.container { max-width: 1180px; margin: 0 auto; padding: 0 24px; }

/* ════════════════════════════════════════
   HERO
   ════════════════════════════════════════ */
.hero {
  position: relative;
  padding: 88px 0 0;
  background: #0a0a0a;
  overflow: hidden;
  color: #fff;
}
.hero-mesh {
  position: absolute; inset: 0;
  background:
    radial-gradient(ellipse 80% 60% at 20% 0%, rgba(185,28,28,0.45), transparent 60%),
    radial-gradient(ellipse 60% 50% at 80% 30%, rgba(217,119,6,0.18), transparent 60%),
    radial-gradient(ellipse 100% 80% at 50% 100%, rgba(127,29,29,0.6), transparent 70%);
  pointer-events: none;
}
.hero-grain {
  position: absolute; inset: 0;
  background-image:
    linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
  background-size: 64px 64px;
  mask-image: radial-gradient(ellipse 100% 70% at 50% 30%, #000 40%, transparent 85%);
  -webkit-mask-image: radial-gradient(ellipse 100% 70% at 50% 30%, #000 40%, transparent 85%);
  pointer-events: none;
}
.hero-content { position: relative; z-index: 2; text-align: center; max-width: 820px; margin-left: auto; margin-right: auto; }

.hero-badge {
  display: inline-flex; align-items: center; gap: 10px;
  padding: 8px 18px; border-radius: 50px;
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.12);
  backdrop-filter: blur(16px);
  font-size: 12.5px; font-weight: 600;
  color: rgba(255,255,255,0.9);
  margin-bottom: 28px; letter-spacing: 0.2px;
}
.pulse-dot {
  width: 7px; height: 7px; border-radius: 50%;
  background: #22c55e;
  box-shadow: 0 0 0 0 rgba(34,197,94,0.7);
  animation: pulse 2s infinite;
}
@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(34,197,94,0.6); }
  70% { box-shadow: 0 0 0 10px rgba(34,197,94,0); }
  100% { box-shadow: 0 0 0 0 rgba(34,197,94,0); }
}

.hero-title {
  font-size: 62px;
  font-weight: 800;
  line-height: 1.06;
  letter-spacing: -2px;
  margin: 0 0 20px;
  color: #fff;
}
.hero-accent {
  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 50%, #d97706 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  font-style: italic;
  padding-right: 6px;
}

.hero-sub {
  font-size: 17.5px;
  color: rgba(255,255,255,0.72);
  max-width: 560px;
  margin: 0 auto 36px;
  line-height: 1.6;
  font-weight: 400;
}

.hero-search {
  display: flex;
  align-items: center;
  gap: 8px;
  max-width: 640px;
  margin: 0 auto 20px;
  padding: 8px 8px 8px 20px;
  background: rgba(255,255,255,0.98);
  border-radius: 16px;
  box-shadow:
    0 24px 60px -20px rgba(0,0,0,0.6),
    0 8px 24px -8px rgba(0,0,0,0.4),
    inset 0 1px 0 rgba(255,255,255,0.8);
  backdrop-filter: blur(24px);
}
.hs-icon { color: #94a3b8; font-size: 15px; flex-shrink: 0; }
.hero-search input {
  flex: 1;
  border: 0;
  padding: 14px 4px;
  font-size: 15.5px;
  font-family: inherit;
  font-weight: 500;
  color: var(--ink);
  background: transparent;
  outline: none;
  min-width: 0;
}
.hero-search input::placeholder { color: #94a3b8; font-weight: 400; }
.hero-search button {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 14px 24px;
  border: 0; border-radius: 11px;
  background: linear-gradient(135deg, #b91c1c, #7f1d1d);
  color: #fff;
  font-family: inherit;
  font-size: 14.5px; font-weight: 700;
  cursor: pointer;
  transition: all 0.2s var(--tr);
  box-shadow: 0 8px 20px -6px rgba(185,28,28,0.5);
  letter-spacing: -0.1px;
  white-space: nowrap;
}
.hero-search button i { font-size: 12px; transition: transform 0.2s; }
.hero-search button:hover { transform: translateY(-1px); box-shadow: 0 12px 26px -6px rgba(185,28,28,0.6); }
.hero-search button:hover i { transform: translateX(2px); }

.hero-quick {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
  gap: 8px;
  font-size: 13px;
  color: rgba(255,255,255,0.55);
  margin-bottom: 60px;
}
.hero-quick-label { font-weight: 600; }
.hero-quick a {
  color: rgba(255,255,255,0.85);
  text-decoration: none;
  padding: 5px 12px;
  border-radius: 50px;
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.1);
  font-weight: 600;
  font-size: 12.5px;
  transition: all 0.2s;
}
.hero-quick a:hover {
  background: rgba(255,255,255,0.14);
  border-color: rgba(255,255,255,0.25);
  text-decoration: none;
  transform: translateY(-1px);
}

/* Trust strip */
.trust-strip {
  position: relative;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: space-around;
  gap: 20px;
  padding: 28px 0 36px;
  border-top: 1px solid rgba(255,255,255,0.08);
  flex-wrap: wrap;
}
.trust-item { text-align: center; min-width: 100px; }
.trust-num {
  font-size: 30px;
  font-weight: 800;
  line-height: 1;
  letter-spacing: -1px;
  background: linear-gradient(180deg, #fff 0%, rgba(255,255,255,0.7) 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
}
.trust-num span { color: #fbbf24; -webkit-text-fill-color: #fbbf24; }
.trust-lbl {
  font-size: 10.5px;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: rgba(255,255,255,0.5);
  font-weight: 700;
  margin-top: 8px;
}
.trust-div { width: 1px; height: 36px; background: rgba(255,255,255,0.1); }

/* ════════════════════════════════════════
   SECTIONS
   ════════════════════════════════════════ */
.section { padding: 88px 0; }
.section:nth-child(even) { background: var(--paper); }
.section-expert { background: linear-gradient(180deg, #fefcf8 0%, #fff 100%) !important; }

.sec-head {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  gap: 20px;
  margin-bottom: 44px;
  flex-wrap: wrap;
}
.eyebrow {
  display: inline-block;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--brand);
  margin-bottom: 12px;
  padding: 5px 12px;
  background: var(--brand-bg);
  border-radius: 50px;
  border: 1px solid #fecaca;
}
.eyebrow-gold { color: var(--gold-dark); background: var(--gold-bg); border-color: #fde68a; }

.sec-title {
  font-size: 40px;
  font-weight: 800;
  color: var(--ink);
  letter-spacing: -1.2px;
  line-height: 1.1;
  margin: 0 0 8px;
}
.sec-title em {
  font-style: italic;
  background: linear-gradient(135deg, #b45309, #d97706);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  padding-right: 4px;
}
.sec-sub {
  font-size: 15px;
  color: var(--muted);
  margin: 0;
  font-weight: 500;
  max-width: 520px;
  line-height: 1.55;
}

.sec-link {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 11px 20px;
  border-radius: 50px;
  background: #fff;
  border: 1.5px solid var(--line);
  color: var(--ink);
  font-size: 13.5px; font-weight: 700;
  text-decoration: none;
  transition: all 0.2s var(--tr);
  letter-spacing: -0.1px;
  white-space: nowrap;
}
.sec-link i { font-size: 11px; transition: transform 0.2s; }
.sec-link:hover {
  background: var(--brand);
  color: #fff;
  border-color: var(--brand);
  text-decoration: none;
  transform: translateY(-1px);
  box-shadow: var(--shadow-brand);
}
.sec-link:hover i { transform: translateX(3px); }
.sec-link-gold:hover {
  background: var(--gold-dark);
  border-color: var(--gold-dark);
  box-shadow: var(--shadow-gold);
}

/* ════════════════════════════════════════
   CATEGORIES
   ════════════════════════════════════════ */
.cat-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(148px, 1fr));
  gap: 14px;
}
.cat-card {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 24px 14px 20px;
  background: #fff;
  border: 1.5px solid var(--line-2);
  border-radius: 18px;
  text-decoration: none;
  transition: all 0.28s var(--tr-out);
  overflow: hidden;
}
.cat-card::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(185,28,28,0.02) 0%, transparent 40%);
  opacity: 0;
  transition: opacity 0.3s;
  pointer-events: none;
}
.cat-card:hover {
  transform: translateY(-6px);
  border-color: #fecaca;
  box-shadow: 0 20px 40px -18px rgba(185,28,28,0.25), 0 8px 16px -8px rgba(15,23,42,0.05);
  text-decoration: none;
}
.cat-card:hover::before { opacity: 1; }

.cat-icon-wrap {
  width: 56px; height: 56px;
  border-radius: 16px;
  display: flex; align-items: center; justify-content: center;
  background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
  color: var(--brand);
  font-size: 23px;
  transition: all 0.3s var(--tr-out);
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.6);
  position: relative;
}
.cat-card:hover .cat-icon-wrap {
  background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
  color: #fff;
  transform: scale(1.1) rotate(-6deg);
  box-shadow: 0 8px 20px -6px rgba(185,28,28,0.4), inset 0 1px 0 rgba(255,255,255,0.2);
}
.cat-name {
  font-size: 12.5px;
  font-weight: 700;
  color: var(--ink-2);
  text-align: center;
  line-height: 1.3;
  letter-spacing: -0.1px;
}

.cat-more {
  text-align: center;
  margin-top: 36px;
}
.btn-ghost {
  display: inline-flex; align-items: center; gap: 10px;
  padding: 13px 26px;
  background: #fff;
  border: 1.5px solid var(--line);
  border-radius: 50px;
  color: var(--ink);
  font-size: 14px;
  font-weight: 700;
  text-decoration: none;
  transition: all 0.25s var(--tr);
}
.btn-ghost i { font-size: 11px; transition: transform 0.25s; }
.btn-ghost:hover {
  background: var(--ink);
  color: #fff;
  border-color: var(--ink);
  text-decoration: none;
  transform: translateY(-2px);
  box-shadow: 0 12px 24px -8px rgba(15,23,42,0.35);
}
.btn-ghost:hover i { transform: translateX(3px); }

/* ════════════════════════════════════════
   EXPERT SALLA
   ════════════════════════════════════════ */
.ex-featured {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: 32px;
  padding: 40px;
  background: linear-gradient(135deg, #0f172a 0%, #1e293b 70%, #334155 100%);
  border-radius: 24px;
  color: #fff;
  margin-bottom: 24px;
  position: relative;
  overflow: hidden;
  box-shadow: 0 32px 64px -24px rgba(15,23,42,0.4);
}
.ex-featured::before {
  content: '';
  position: absolute;
  top: -50%; right: -20%;
  width: 500px; height: 500px;
  background: radial-gradient(circle, rgba(251,191,36,0.18), transparent 60%);
  border-radius: 50%;
  pointer-events: none;
}
.ex-featured::after {
  content: '';
  position: absolute;
  bottom: -60%; left: -20%;
  width: 400px; height: 400px;
  background: radial-gradient(circle, rgba(185,28,28,0.35), transparent 60%);
  border-radius: 50%;
  pointer-events: none;
}
.ex-featured > * { position: relative; z-index: 2; }

.ex-featured-pill {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 6px 14px;
  border-radius: 50px;
  background: rgba(251,191,36,0.15);
  border: 1px solid rgba(251,191,36,0.35);
  color: #fbbf24;
  font-size: 11px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 18px;
}
.ex-featured-title {
  font-size: 32px;
  font-weight: 800;
  line-height: 1.15;
  letter-spacing: -0.8px;
  margin: 0 0 14px;
  color: #fff;
}
.ex-featured-title em {
  font-style: italic;
  color: #fbbf24;
  font-weight: 700;
}
.ex-featured-desc {
  font-size: 15px;
  line-height: 1.65;
  color: rgba(255,255,255,0.72);
  margin: 0 0 22px;
  max-width: 480px;
}
.ex-featured-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin-bottom: 24px;
  font-size: 12.5px;
  font-weight: 600;
  color: rgba(255,255,255,0.6);
}
.ex-featured-meta span { display: inline-flex; align-items: center; gap: 7px; }
.ex-featured-meta i { color: #fbbf24; font-size: 11.5px; }

.ex-featured-btn {
  display: inline-flex; align-items: center; gap: 9px;
  padding: 13px 24px;
  background: linear-gradient(135deg, #fbbf24, #d97706);
  color: #0f0a0a;
  border-radius: 12px;
  font-size: 13.5px;
  font-weight: 800;
  text-decoration: none;
  box-shadow: 0 12px 28px -8px rgba(212,160,23,0.6);
  transition: all 0.2s var(--tr);
}
.ex-featured-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 16px 34px -8px rgba(212,160,23,0.7);
  text-decoration: none;
}
.ex-featured-btn i { font-size: 12px; }

.ex-featured-right {
  display: flex;
  flex-direction: column;
  gap: 12px;
  justify-content: center;
}
.ex-stat {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 16px 18px;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 14px;
  backdrop-filter: blur(10px);
  transition: all 0.2s;
}
.ex-stat:hover {
  background: rgba(255,255,255,0.08);
  border-color: rgba(251,191,36,0.3);
  transform: translateX(4px);
}
.ex-stat i {
  width: 40px; height: 40px;
  border-radius: 11px;
  background: rgba(251,191,36,0.15);
  color: #fbbf24;
  font-size: 17px;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.ex-stat-num {
  font-size: 20px;
  font-weight: 800;
  color: #fff;
  letter-spacing: -0.3px;
  line-height: 1;
}
.ex-stat-lbl {
  font-size: 11px;
  font-weight: 700;
  color: rgba(255,255,255,0.55);
  text-transform: uppercase;
  letter-spacing: 0.8px;
  margin-top: 4px;
}

/* Expert cards grid */
.ex-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 14px;
  margin-bottom: 32px;
}
.ex-card {
  display: flex;
  gap: 14px;
  padding: 18px;
  background: #fff;
  border: 1.5px solid var(--line-2);
  border-radius: 16px;
  text-decoration: none;
  transition: all 0.25s var(--tr-out);
  position: relative;
  overflow: hidden;
}
.ex-card::after {
  content: '';
  position: absolute;
  bottom: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, #dc2626, #f59e0b);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.35s var(--tr-out);
}
.ex-card:hover {
  transform: translateY(-4px);
  border-color: #fde68a;
  box-shadow: 0 16px 36px -16px rgba(15,23,42,0.12), 0 6px 14px -6px rgba(217,119,6,0.08);
  text-decoration: none;
}
.ex-card:hover::after { transform: scaleX(1); }

.ex-card-av {
  width: 48px; height: 48px;
  border-radius: 13px;
  display: flex; align-items: center; justify-content: center;
  color: #fff;
  font-size: 19px;
  flex-shrink: 0;
  box-shadow: 0 6px 16px -6px rgba(15,23,42,0.28), inset 0 1px 0 rgba(255,255,255,0.2);
}
.ex-card-body { flex: 1; min-width: 0; display: flex; flex-direction: column; }
.ex-card-name {
  font-size: 15px;
  font-weight: 800;
  color: var(--ink);
  letter-spacing: -0.3px;
  margin-bottom: 4px;
}
.ex-card-sample {
  font-size: 12.5px;
  color: var(--slate);
  line-height: 1.5;
  font-style: italic;
  font-weight: 500;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  margin-bottom: 10px;
  flex: 1;
}
.ex-card-foot {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  padding-top: 10px;
  border-top: 1px solid var(--line-2);
}
.ex-card-cat {
  font-size: 10px;
  font-weight: 800;
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.6px;
}
.ex-card-read {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 11px;
  font-weight: 800;
  color: var(--brand);
  transition: gap 0.2s;
}
.ex-card:hover .ex-card-read { gap: 9px; }
.ex-card-read i { font-size: 9.5px; }

/* Sponsor CTA */
.ex-cta {
  position: relative;
  padding: 28px 32px;
  background: linear-gradient(135deg, #7f1d1d 0%, #450a0a 100%);
  border-radius: 20px;
  overflow: hidden;
  box-shadow: 0 24px 48px -20px rgba(127,29,29,0.5);
}
.ex-cta-glow {
  position: absolute;
  top: -50%; right: -10%;
  width: 400px; height: 400px;
  background: radial-gradient(circle, rgba(251,191,36,0.25), transparent 60%);
  border-radius: 50%;
  pointer-events: none;
}
.ex-cta-inner {
  position: relative;
  z-index: 2;
  display: flex;
  align-items: center;
  gap: 22px;
  flex-wrap: wrap;
}
.ex-cta-icon {
  width: 56px; height: 56px;
  border-radius: 16px;
  background: rgba(251,191,36,0.15);
  border: 1px solid rgba(251,191,36,0.35);
  display: flex; align-items: center; justify-content: center;
  color: #fbbf24;
  font-size: 22px;
  flex-shrink: 0;
}
.ex-cta-text { flex: 1; min-width: 220px; }
.ex-cta-text h4 {
  font-size: 18px;
  font-weight: 800;
  color: #fff;
  margin: 0 0 6px;
  letter-spacing: -0.3px;
}
.ex-cta-text p {
  font-size: 13.5px;
  color: rgba(255,255,255,0.72);
  margin: 0;
  line-height: 1.55;
}
.ex-cta-btn {
  display: inline-flex; align-items: center; gap: 9px;
  padding: 13px 24px;
  background: #fff;
  color: #7f1d1d;
  border-radius: 11px;
  font-size: 13.5px;
  font-weight: 800;
  text-decoration: none;
  transition: all 0.2s var(--tr);
  white-space: nowrap;
  box-shadow: 0 12px 28px -8px rgba(0,0,0,0.3);
}
.ex-cta-btn:hover {
  transform: translateY(-2px);
  text-decoration: none;
  box-shadow: 0 16px 34px -8px rgba(0,0,0,0.4);
}
.ex-cta-btn i { font-size: 11px; transition: transform 0.2s; }
.ex-cta-btn:hover i { transform: translateX(3px); }

/* ════════════════════════════════════════
   AREAS
   ════════════════════════════════════════ */
.loc-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
  gap: 10px;
}
.loc-chip {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 18px;
  background: #fff;
  border: 1.5px solid var(--line-2);
  border-radius: 12px;
  text-decoration: none;
  transition: all 0.2s var(--tr);
  position: relative;
  overflow: hidden;
}
.loc-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  background: var(--brand);
  flex-shrink: 0;
  transition: all 0.2s;
  position: relative;
}
.loc-dot::after {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 50%;
  border: 1px solid var(--brand);
  opacity: 0;
  transition: all 0.25s;
}
.loc-name {
  flex: 1;
  font-size: 13.5px;
  font-weight: 700;
  color: var(--ink-2);
  letter-spacing: -0.1px;
  transition: color 0.2s;
}
.loc-arrow {
  font-size: 11px;
  color: var(--muted-2);
  opacity: 0;
  transform: translateX(-4px);
  transition: all 0.2s;
}
.loc-chip:hover {
  border-color: #fecaca;
  background: linear-gradient(135deg, #fff 0%, #fef2f2 100%);
  text-decoration: none;
  transform: translateY(-2px);
  box-shadow: 0 12px 24px -12px rgba(185,28,28,0.2);
}
.loc-chip:hover .loc-dot::after { opacity: 1; inset: -5px; }
.loc-chip:hover .loc-name { color: var(--brand); }
.loc-chip:hover .loc-arrow {
  opacity: 1;
  transform: translateX(0);
  color: var(--brand);
}

/* ════════════════════════════════════════
   FINAL CTA
   ════════════════════════════════════════ */
.final-cta {
  position: relative;
  padding: 72px 40px 68px;
  background: #0a0a0a;
  border-radius: 32px;
  text-align: center;
  overflow: hidden;
  box-shadow: 0 40px 80px -32px rgba(0,0,0,0.5);
}
.final-cta-glow-1 {
  position: absolute;
  top: -50%; left: 30%;
  width: 700px; height: 700px;
  background: radial-gradient(circle, rgba(185,28,28,0.35), transparent 60%);
  border-radius: 50%;
  pointer-events: none;
}
.final-cta-glow-2 {
  position: absolute;
  bottom: -50%; right: 20%;
  width: 500px; height: 500px;
  background: radial-gradient(circle, rgba(217,119,6,0.18), transparent 60%);
  border-radius: 50%;
  pointer-events: none;
}
.final-cta-inner { position: relative; z-index: 2; max-width: 620px; margin: 0 auto; }

.final-icon {
  width: 68px; height: 68px;
  margin: 0 auto 24px;
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.14);
  border-radius: 20px;
  display: flex; align-items: center; justify-content: center;
  font-size: 26px;
  color: #fbbf24;
  backdrop-filter: blur(10px);
}
.final-cta h2 {
  font-size: 42px;
  font-weight: 800;
  color: #fff;
  letter-spacing: -1.2px;
  line-height: 1.1;
  margin: 0 0 16px;
}
.final-cta p {
  font-size: 16px;
  color: rgba(255,255,255,0.7);
  line-height: 1.65;
  margin: 0 0 32px;
}
.final-cta-btns {
  display: flex;
  gap: 12px;
  justify-content: center;
  flex-wrap: wrap;
  margin-bottom: 24px;
}
.btn-primary {
  display: inline-flex; align-items: center; gap: 10px;
  padding: 15px 30px;
  background: linear-gradient(135deg, #dc2626, #991b1b);
  color: #fff;
  border-radius: 13px;
  font-size: 14.5px;
  font-weight: 800;
  text-decoration: none;
  box-shadow: 0 16px 32px -10px rgba(220,38,38,0.6);
  transition: all 0.2s var(--tr);
  letter-spacing: -0.1px;
}
.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 20px 40px -10px rgba(220,38,38,0.7);
  text-decoration: none;
}
.btn-primary i { font-size: 13px; }

.btn-outline {
  display: inline-flex; align-items: center; gap: 10px;
  padding: 15px 30px;
  background: rgba(255,255,255,0.06);
  border: 1.5px solid rgba(255,255,255,0.2);
  color: #fff;
  border-radius: 13px;
  font-size: 14.5px;
  font-weight: 800;
  text-decoration: none;
  transition: all 0.2s var(--tr);
  letter-spacing: -0.1px;
  backdrop-filter: blur(10px);
}
.btn-outline:hover {
  background: rgba(255,255,255,0.14);
  border-color: rgba(255,255,255,0.35);
  text-decoration: none;
  transform: translateY(-2px);
}
.btn-outline i { font-size: 13px; color: #fbbf24; }

.final-cta-note {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 12.5px;
  color: rgba(255,255,255,0.5);
  font-weight: 600;
}
.final-cta-note i { color: #22c55e; font-size: 12px; }

/* ════════════════════════════════════════
   RESPONSIVE
   ════════════════════════════════════════ */
@media (max-width: 1024px) {
  .hero-title { font-size: 52px; }
  .ex-featured { grid-template-columns: 1fr; gap: 24px; }
  .ex-featured-right { flex-direction: row; }
  .ex-stat { flex: 1; }
}
@media (max-width: 768px) {
  .container { padding: 0 18px; }
  .hero { padding: 56px 0 0; }
  .hero-title { font-size: 34px; letter-spacing: -1px; }
  .hero-sub { font-size: 15px; }
  .hero-search { flex-direction: column; padding: 12px; gap: 10px; }
  .hs-icon { display: none; }
  .hero-search input { background: #f8fafc; border-radius: 10px; padding: 14px 16px; width: 100%; }
  .hero-search button { width: 100%; justify-content: center; }
  .hero-quick { margin-bottom: 40px; }
  .trust-strip { padding: 24px 0 28px; gap: 12px; }
  .trust-num { font-size: 22px; }
  .trust-lbl { font-size: 9.5px; letter-spacing: 1px; }
  .trust-div { display: none; }
  .trust-item { min-width: 80px; }

  .section { padding: 56px 0; }
  .sec-title { font-size: 28px; letter-spacing: -0.8px; }
  .sec-sub { font-size: 13.5px; }
  .sec-head { margin-bottom: 28px; }
  .eyebrow { font-size: 10px; padding: 4px 10px; letter-spacing: 1.5px; }

  .cat-grid { grid-template-columns: repeat(auto-fill, minmax(105px, 1fr)); gap: 10px; }
  .cat-card { padding: 18px 10px 14px; border-radius: 14px; }
  .cat-icon-wrap { width: 46px; height: 46px; font-size: 19px; border-radius: 13px; }
  .cat-name { font-size: 11.5px; }

  .ex-featured { padding: 24px; border-radius: 20px; }
  .ex-featured-title { font-size: 22px; }
  .ex-featured-desc { font-size: 13.5px; }
  .ex-featured-meta { gap: 12px; font-size: 11.5px; }
  .ex-featured-right { flex-direction: column; }
  .ex-grid { grid-template-columns: 1fr; gap: 10px; }
  .ex-card { padding: 14px; }

  .ex-cta { padding: 22px; border-radius: 16px; }
  .ex-cta-inner { flex-direction: column; text-align: center; }
  .ex-cta-btn { width: 100%; justify-content: center; }

  .loc-grid { grid-template-columns: repeat(2, 1fr); gap: 8px; }
  .loc-chip { padding: 11px 14px; }
  .loc-name { font-size: 12.5px; }

  .final-cta { padding: 48px 24px; border-radius: 22px; }
  .final-cta h2 { font-size: 26px; letter-spacing: -0.6px; }
  .final-cta p { font-size: 14px; }
  .final-icon { width: 56px; height: 56px; font-size: 22px; border-radius: 16px; }
  .btn-primary, .btn-outline { padding: 13px 22px; font-size: 13.5px; width: 100%; justify-content: center; }
}
@media (max-width: 480px) {
  .hero-title { font-size: 28px; }
  .sec-title { font-size: 24px; }
  .trust-item { min-width: 70px; }
  .trust-num { font-size: 20px; }
}
</style>
